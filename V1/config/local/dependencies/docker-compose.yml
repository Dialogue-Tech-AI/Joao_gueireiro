# ============================================================
# Plataforma Guerreiros - Dependências (DESENVOLVIMENTO)
# ============================================================
# Credenciais alinhadas com backend.local.env e ai-worker.local.env
# Uso: docker compose -f config/local/dependencies/docker-compose.yml up -d
# ============================================================

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: guerreiros-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: guerreiros
      POSTGRES_PASSWORD: guerreiros123
      POSTGRES_DB: guerreiros
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../../core/shared/database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - guerreiros-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U guerreiros -d guerreiros"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache and Pub/Sub
  redis:
    image: redis:7-alpine
    container_name: guerreiros-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - guerreiros-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # RabbitMQ Message Queue
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: guerreiros-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: guerreiros
      RABBITMQ_DEFAULT_PASS: guerreiros123
      RABBITMQ_DEFAULT_VHOST: /
    ports:
      - "5672:5672"   # AMQP protocol
      - "15672:15672" # Management UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - guerreiros-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  # MinIO S3-compatible Storage
  minio:
    image: minio/minio:latest
    container_name: guerreiros-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: guerreiros
      MINIO_ROOT_PASSWORD: guerreiros
    ports:
      - "9000:9000"   # API
      - "9001:9001"   # Console UI
    volumes:
      - minio_data:/data
    networks:
      - guerreiros-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 5

  # MinIO Client - Create buckets on startup
  minio-init:
    image: minio/mc:latest
    container_name: guerreiros-minio-init
    restart: "no"  # Não reiniciar - é um container de inicialização única
    depends_on:
      minio:
        condition: service_healthy  # Aguardar MinIO estar saudável
    networks:
      - guerreiros-network
    entrypoint: >
      /bin/sh -c "
      echo 'Aguardando MinIO estar pronto...';
      sleep 15;
      echo 'Configurando MinIO client...';
      /usr/bin/mc alias set guerreiros http://minio:9000 guerreiros guerreiros || exit 1;
      echo 'Criando buckets...';
      /usr/bin/mc mb --ignore-existing guerreiros/attachments || exit 1;
      /usr/bin/mc mb --ignore-existing guerreiros/media || exit 1;
      /usr/bin/mc mb --ignore-existing guerreiros/logs || exit 1;
      echo 'Configurando permissões...';
      /usr/bin/mc anonymous set download guerreiros/attachments || exit 1;
      /usr/bin/mc anonymous set download guerreiros/media || exit 1;
      echo 'Inicialização concluída com sucesso!';
      exit 0;
      "

  # Qdrant Vector Database
  qdrant:
    image: qdrant/qdrant:latest
    container_name: guerreiros-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"  # HTTP API
      - "6334:6334"  # gRPC API
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - guerreiros-network
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f qdrant || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

# Volumes for data persistence (com prefixo guerreiros- para evitar conflito com outros projetos)
volumes:
  postgres_data:
    name: guerreiros-postgres-data
    driver: local
  redis_data:
    name: guerreiros-redis-data
    driver: local
  rabbitmq_data:
    name: guerreiros-rabbitmq-data
    driver: local
  minio_data:
    name: guerreiros-minio-data
    driver: local
  qdrant_data:
    name: guerreiros-qdrant-data
    driver: local

# Network for inter-service communication (evita conflito com outros projetos)
networks:
  guerreiros-network:
    name: guerreiros-network
    driver: bridge

